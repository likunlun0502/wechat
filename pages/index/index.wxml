<view class="map-con">
	<map id="map" longitude="{{longitude}}" latitude="{{latitude}}" controls="{{controls}}" show-location bindcontroltap="handleControltap" markers="{{markers}}" bindmarkertap="bindMarkerTap" scale="16"  style="width: 100%; height: 100%;"></map>
</view>

<view class="bottom-area">
	<navigator url="/pages/publish/publish" class="publish">发布</navigator>
	<view class="search">搜索</view>
</view>